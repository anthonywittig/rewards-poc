# The build context for deploy/Dockerfile is the repo root, so this is an
# allowlist: everything is excluded, then the paths the worker binary needs
# are added back. .git therefore never reaches the daemon.
*
!go.mod
!go.sum
!cmd
!internal
